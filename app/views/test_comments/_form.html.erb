<%= form_for(@test_comment) do |f| %>
  <% if @test_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_comment.errors.count, "error") %> prohibited this test_comment from being saved:</h2>

      <ul>
      <% @test_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
